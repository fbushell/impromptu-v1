{
  "name": "dyna-reem",
  "version": "0.0.0",
  "description": "The SASS directory is located one level above the theme folder so we don't clutter up the `assets/` folder. We preproccess the .sass files from the `sass/` folder into the theme's `assets/` folder.",
  "main": "Gruntfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cirroweb/impromptu-ss.git"
  },
  "author": "",
  "license": "BSD-2-Clause",
  "bugs": {
    "url": "https://github.com/cirroweb/impromptu-ss/issues"
  },
  "devDependencies": {
    "grunt": "~0.4.1",
    "grunt-contrib-sass": "~0.3.0",
    "grunt-contrib-watch": "~0.4.4",
    "grunt-autoprefixer": "~1.0.1"
  }
}
